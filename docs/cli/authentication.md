# Authentication Setup

本系统使用 Cheeth OA 代理模式进行认证，无需复杂配置。

## Cheeth OA (代理模式)

**默认且唯一的认证方式**

- 系统会自动使用内置的代理服务器配置
- 无需用户配置任何环境变量或API密钥  
- 首次启动时会自动选择此认证方式
- 认证成功后会自动设置 Claude 模型 (`claude-sonnet-4@20250514`)

## 使用说明

1. **首次启动**: 系统会自动选择 Cheeth OA 认证方式
2. **认证流程**: 无需手动操作，系统会自动处理认证
3. **模型配置**: 认证成功后会自动配置为使用 Claude 模型

## 故障排除

如果遇到认证问题，请：

1. 确保网络连接正常
2. 检查代理服务器是否可访问
3. 如有持续问题，请使用 `/bug` 命令反馈问题

## 配置持久化

系统会自动保存认证配置，无需手动配置。所有设置都会保存在用户配置文件中，下次启动时会自动加载。